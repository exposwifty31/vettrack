# Replaces a broken GitHub Actions registration at this path (startup_failure).
# Canonical CI lives in .github/workflows/ci.yml — do not add jobs here.
name: "[disabled] legacy BuildFailed placeholder"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder — use CI — VetTrack workflow"
