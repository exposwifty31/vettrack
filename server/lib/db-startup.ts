/**\n * Database startup initialization.\n * Call once from server/index.ts before routes are mounted.\n */\nimport { pool } from \"../db.js\";\nimport { initQueryTimingHook } from \"./query-timing-hook.js\";\n\nexport function initDatabaseMonitoring(): void {\n  try {\n    initQueryTimingHook(pool);\n    console.log(\"✅ Database query monitoring enabled\");\n  } catch (err) {\n    console.warn(\"[db] failed to initialize query monitoring:\", (err as Error).message);\n  }\n}\n"
