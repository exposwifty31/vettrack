# Audit + Verify — Program Phases 0–6

**Date:** 2026-07-08 · **Scope:** `docs/design/program-plan.md` Phases 0–6 (Waves 1–2) · **Trigger:** pre-Phase-7 gate
**Method:** every deliverable checked against real code (grep / read `file:line`); every health claim backed by a command run this session. No claim is inferred from a green unit test alone (program rule III.6).

---

## Verdict

**Phases 0–6 are implemented and health-green.** All claimed deliverables exist in code. No blocking (Critical/Major) defect found. The findings are **debt and doc-drift**, not broken flows — routed to [`TECH_DEBT_REGISTER.md`](./TECH_DEBT_REGISTER.md).

> Caveat (honest scope): this is a **static + build-gate** audit — typecheck, unit/integration tests, dead-code and dependency analysis, and code-presence verification. It is **not** the III.6 live four-platform flow walk (owner accepted deferring that to Phase 10). Green here means "compiles, types, tests pass, deliverables present"; it does **not** prove every flow runs in a real browser/sim.

---

## Health gates (run 2026-07-08)

| Gate | Command | Result |
|------|---------|--------|
| Types | `pnpm typecheck` | ✅ exit 0 (frontend + server tsconfigs) |
| Tests | `pnpm test` | ✅ **418 files, 4038 passed, 0 failed** (37s) |
| i18n parity | `pnpm i18n:check` | ✅ `en.json`/`he.json` deep key parity |
| Architecture | `pnpm architecture:gates` | ✅ exit 0 (dependency-cruiser baseline + madge cycles) |
| Dead code | `pnpm knip` | ⚠️ exit 1 — findings (not part of `architecture:gates`; non-blocking) → TECH_DEBT |

## Deliverable verification (Phases 0–6)

| Phase | Claimed deliverable | Evidence (`file:line` / command) | Status |
|-------|--------------------|----------------------------------|--------|
| **0** | Dev-role switcher (`vt:devRole` → `x-dev-role-override`, inert in Clerk) | `src/lib/auth-fetch.ts:24,96-98`, `src/features/settings/DevRoleSwitcher.tsx:25-65` | ✅ present |
| **0** | Baseline audits | `docs/audit/RELEVANCE_BASELINE.md`, `docs/audit/FLOW_INVENTORY.md` | ✅ present |
| **1** | Web-management design brief + handoff | `docs/design/web-management-brief.md`, `VetTrack Design System - Phase 1 .zip` | ✅ present |
| **2** | Experience model (7 roles → 5 archetypes, total map; `Capability` union; `can()`) | `src/lib/roles/experience-model.ts:17,33,70,202`, `src/hooks/use-experience.ts` | ✅ present |
| **3** | Home split fork (ops vs floor) + surfaces + i18n | `src/pages/home.tsx:21-30`, `src/features/today/surfaces/{OpsHomeSurface,FloorHomeSurface}.tsx`, `homeSurface.*` in both locales | ✅ present |
| **4** | 4th `PlatformTarget "board"` + `isBoardPathname` in both resolvers | `src/app/platform/index.ts:6,35,61,87` | ✅ present |
| **4** | `BoardShell` kiosk host + `command-board/` extraction + `/board` route (AuthGuard, **no** WebOnlyGuard) | `src/board/{BoardShell,BoardErrorBoundary,KioskAwake,useBoardAutoReload}`, `src/features/command-board/CommandBoardScreen.tsx`, `routes.tsx:126` | ✅ present |
| **5** | Additive optional snapshot blocks (`power?/docks?/waitlist?/staging?`) + `byLocation` populated | `shared/equipment-board.ts:139,157-160` | ✅ present |
| **5** | Panels + calm/pressure mode | `src/features/command-board/components/board-panels.tsx:46-77`, `use-board-mode.ts` | ✅ present |
| **6** | Web-management nav model + 5 scaffold routes (WebOnlyGuard + ManagementGuard) | `src/lib/routes/web-management-nav-model.ts`, `routes.tsx:171-175` | ✅ present |
| **6** | Desktop management primitives | `src/desktop/management/{DataTable,ManagementGuard,PendingConsolePage,ReadOnlyChip}.tsx` | ✅ present |

## Findings

Severity: **Critical** (block) · **Major** (fix soon) · **Minor** (maintenance) · **Info** (note).

| # | Finding | Severity | Location | Fix |
|---|---------|----------|----------|-----|
| A-1 | **14 test blocks never execute.** `server/tests/*.test.ts` matches no runner's include glob (`vite.config.ts:128` globs `tests/**` + `src/**`; no config/script references `server/tests`). Silent zero-coverage that reads as coverage. | **Major** | `server/tests/security.test.ts` (11), `server/tests/shift-chat.test.ts` (3) | Wire into a vitest include, or delete if superseded by `tests/**` equivalents |
| A-2 | **25 unused prod deps + 5 devDeps** (0 imports confirmed for `framer-motion`, `zustand`, `fuse.js`, `react-virtuoso`, 14 `@radix-ui/*`, `pdf-parse`, `@clerk/clerk-sdk-node`, `@ionic/*`). Install bloat + supply-chain surface. | Minor | `package.json` | Careful removal pass (some radix back unused `components/ui/*`) |
| A-3 | **200 unused exports + 203 unused exported types.** Mix of in-progress hexagonal barrels (`src/core/index.ts`, `src/infrastructure/*`, `src/shell/*`) and removed-scope (`src/types/billing.ts`). | Minor | knip report | Classify: migration-scaffold (keep) vs dead (delete) |
| A-4 | **Plan ↔ code drift.** program-plan.md Phase 6 says "also fence `/admin/metrics`"; code deliberately left it `AuthGuard`-only (real mobile screen) — logged in PROOF_ALIGNMENT_LOG:1527/1531 as "dropped, separate ticket", **but the plan text still reads as an open task.** | Minor | `routes.tsx:169` vs plan §239 | Reconcile plan (or the "separate ticket") so the deviation is visible in the plan of record |
| A-5 | **Stale `MAINTENANCE_MODE.md`.** Frames the repo as frozen maintenance; contradicts an active 10-phase program. Already flagged in plan §334 ("update when owner ready"). | Minor | `docs/MAINTENANCE_MODE.md` | Owner-gated update |
| A-6 | **Generated inventories were stale** (Jun-18, pre-board/console). | Info (**fixed this session**) | `docs/audit/{db,routes,frontend-routes}.md` | ✅ regenerated via `pnpm docs:audit` (now 249 routes, 64 tables) |
| A-7 | **Stale `knip.json` ignore entries** (`src/lib/tokens.ts`, `server/seed.ts` now used → "Remove from ignore"). | Info | `knip.json` | Drop the two stale ignores |
| A-8 | **No `docs/CODEMAPS/`** for AI/onboarding context. | Info (**fixed this session**) | — | ✅ created `docs/CODEMAPS/{architecture,backend,frontend,data,dependencies}.md` |

**Not defects — clean signals worth recording:**
- Zero `TODO/FIXME/HACK/@ts-ignore/eslint-disable` markers in any Phase 0–6 surface (`src/board/`, `command-board/`, `today/surfaces/`, `roles/`, `desktop/management/`, the snapshot service). The new code carries no debt markers.
- Every Phase 0–6 route with a real mobile screen is reachable (no over-fencing regression); `/board` correctly `AuthGuard`-only per spec.

## Priority actions
1. **A-1 (Major) — wire or delete the 14 dead server tests.** A security test that never runs is worse than no test: it signals coverage that isn't there. One-line fix if wired; verify each still passes before trusting it.
2. **A-4 / A-5 (doc drift) — reconcile plan-of-record before Phase 7 branches off it.** Phase 7 reads the plan as truth; a stale "fence /admin/metrics" line risks a wrong-headed edit.
3. **A-2 / A-3 (dead weight) — schedule a clean sub-phase.** Best folded into the Phase 7 mandatory closing clean (III.7) rather than a standalone churn PR.

_See [`TECH_DEBT_REGISTER.md`](./TECH_DEBT_REGISTER.md) for scored prioritization and the deferred-backlog carryover._
