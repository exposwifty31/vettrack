// Client-facing notification-delivery row (Phase 7b). Masked target only — the
// raw endpoint/keys/phone never reach the client (server masks them).
export type { NotificationDeliveryRow, NotificationChannel } from "../../shared/notification-delivery.js";
