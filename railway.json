{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NODE_ENV=development pnpm install --frozen-lockfile && echo \">>> build-env: VITE_PILOT_MODE=$VITE_PILOT_MODE\" && VITE_CLERK_PUBLISHABLE_KEY=$VITE_CLERK_PUBLISHABLE_KEY VITE_PILOT_MODE=$VITE_PILOT_MODE pnpm build"
  },
  "deploy": {
    "startCommand": "pnpm start"
  }
}